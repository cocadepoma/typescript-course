(() => {
  // const numbers: (string | number | boolean)[] = [1, 2, '3', 4, 5];
  const numbers: number[] = [1, 2, 3, 4, 5];

  const villians = ['Dr Strange', 'Hulk', 'Spiderman'];
  villians.forEach((villian) => console.log(villian.toUpperCase()));

  console.log(numbers);
})();